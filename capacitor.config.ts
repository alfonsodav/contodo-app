import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'huawei.spicyrocket.contodo',
  appName: 'contodo-app',
  webDir: 'www',
  bundledWebRuntime: false,
  plugins: {},
};

export default config;
